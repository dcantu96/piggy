<%= form_for(goal) do |f| %>
    <% if goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goal.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% goal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br/>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br/>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :completion_date %><br/>
    <%= f.text_field :completion_date %>
  </div>
  <%= f.hidden_field :user_id, value: goal.user.id %>
  <div class="actions">
    <%= f.submit %>
  </div>
        
<% end %>
  