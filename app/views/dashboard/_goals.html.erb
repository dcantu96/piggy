<% @goals.in_groups_of(3).each do |group| %>
    <div class="row">
        <% group.each do |goal| %>
            <div class="col">
                <%= render 'dashboard/goal_element', goal: goal if goal %>
            </div>
        <% end %>
    </div>
<% end %>